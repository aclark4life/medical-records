# Moved to medical_records.django_only.models — kept for backward compatibility.
from medical_records.django_only.models import Patient, PatientRecord, Billing

__all__ = ["Patient", "PatientRecord", "Billing"]
